module.exports = {
  // Jest has a very flexible test regex, but that flexibility decreases
  // standardization between packages and common use cases such as test utils
  // stored alongside test files.
  testRegex: '/__tests__/.+\\.(test|spec)\\.[jt]sx?$',
}
